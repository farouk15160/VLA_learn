[pytest]
testpaths = tests
pythonpath = .
# ROS 2 exposes pytest plugins globally through PYTHONPATH. They are unrelated
# to this repository and can fail during pytest startup when their dependencies
# or hook API do not match the active virtual environment.
addopts = -p no:launch_testing -p no:launch_ros
