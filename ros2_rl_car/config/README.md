# Configuration

`default.json` records the long-run PPO and checkpoint defaults. CLI arguments
override file values, making short smoke-training and alternate-seed runs easy
without editing the reproducible default. Paths are relative to the project
directory when commands are run through `scripts/rl_car`.
