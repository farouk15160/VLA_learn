# Simulator assets

`worlds/` contains the generated Gazebo Classic circuit and the matching centre
line used by reward, dashboard, and evaluator code. Assets are deterministic
and have no downloaded model dependencies.
